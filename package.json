{
  "name": "DerbyHeatWebsite",
  "version": "1.0.0",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build && CI=false && npm install && npm run build",
    "generate": "nuxt generate",
    "start": "nuxt dev"
  },
  "dependencies": {
    "@stackbit/cms-git": "^1.0.32",
    "@stackbit/types": "^2.1.12",
    "nuxt": "^3.15.4"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
